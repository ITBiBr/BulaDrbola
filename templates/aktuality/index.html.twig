{% extends 'base.html.twig' %}

{% block title %}Aktuality{% endblock %}

{% block body %}
<div class = "container-fluid pt-5 mt-3">
    <div class="row text-center">
        <div class="col-12 p-3">
            <h1 class="text-primary fw-bold text-uppercase">Aktuality</h1>
        </div>
    </div>


    <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 g-4 px-lg-5 py-lg-3">

        <!-- Aktualita 7
        <div class="col">
            <div class="card h-100 border-0 rounded-0">
                <div class="card-header bg-white border-0 pb-0">
                    <small class="text-muted">5. srpna 2025</small>
                </div>
                <div class="px-3">
                <div class="image-overlay">
                    <img src="{{ asset('images/aktuality/07.jpg') }}" class="card-img-top card-img-top-filter rounded-0" alt="Náhledový obrázek">
                </div>
                </div>
                <div class="card-body">
                    <h5 class="card-title mt-2 text-primary fw-bold">Barevná aktualita</h5>
                    <p class="card-text mb-0">Zde je použita barevná fotografie</p>
                    <a href="#" class="fw-bold">Číst více</a>
                </div>
            </div>
        </div>-->
        {% for aktualita in aktuality %}
            <div class="col">
                <div class="card h-100 border-0 rounded-0">
                    <div class="card-header bg-white border-0 pb-0">
                        <small class="text-muted">{{ aktualita.Datum|cesky_datum }}</small>
                    </div>
                    <div class="px-3">
                        <img src="images/aktuality/{{ aktualita.Obrazek }}" class="card-img-top rounded-0" alt="Náhledový obrázek">
                    </div>
                    <div class="card-body">
                        <h5 class="card-title mt-2 text-primary fw-bold">{{ aktualita.Titulek}}</h5>
                        <p class="card-text mb-0">{{ aktualita.Perex|raw }}</p>
                        <a href="#" class="fw-bold">Číst více</a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    <div class="container-fluid text-center">
        <div class="row justify-content-center">
            <div class="col-md-12">
                <button class="btn btn-outline-primary rounded-pill m-5"><div class="mx-3">Načíst další</div></button>
            </div>
        </div>
    </div>



</div>
{% endblock %}
